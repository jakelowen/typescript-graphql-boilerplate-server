type RegisterResponse {
  error: [Error!]
  register: String
}

input registerInput {
  email: String!
  password: String!
  firstName: String
  lastName: String
}

type Mutation {
  register(input: registerInput!): RegisterResponse!
}
